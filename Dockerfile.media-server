FROM alfg/nginx-rtmp:latest

# Copy custom nginx config (RTMP ingest + HLS output)
COPY media-server/nginx.conf /etc/nginx/nginx.conf

# Ensure HLS output directory exists
RUN mkdir -p /var/www/hls/live

EXPOSE 1935 8000

CMD ["nginx", "-g", "daemon off;"]
